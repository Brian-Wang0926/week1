
1.Task1
主要是透過.item的方式去取出鍵、值再判斷。
2.Task2
卡比較久的部份是輸入的字串要為數字型態，python可以透過try、except處理；JS則是透過正規表示式還有.test去測試是否符合條件。
計算薪資的部分要先判斷字串是否只包含數字符號，python可以透過isdigit()去判斷；JS可以透過!isNaN去判斷。
3.Task3
迴圈包迴圈加上IF的判斷，邊寫邊印出來，確定想法對不對。